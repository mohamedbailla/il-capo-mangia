import AdminPageClient from "./AdminPageClient";

export const metadata = {
  title: "Panel de Administración — IL Capo Mangia",
};

export default function AdminPage() {
  return <AdminPageClient />;
}
